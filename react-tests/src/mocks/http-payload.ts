import { IAuthUser } from "../common/types";

// Schema for HTTP Request / Response
export const loginPayload: IAuthUser = {
    name: 'John Doe',
    userName: 'john@doe.com',
    _id: 'libcv965415412'
}